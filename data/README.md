Drop the DMI assignment workbook here as:

`digimon_role_assignment_all_forms_new.pdf`

Parsed sheet: `src/lib/sheet.json`. Live editable database: `catalog.json` (created on first run, then updated from `/admin`).

